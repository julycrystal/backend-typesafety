type Query {
  hello: String
  tweets: [Tweet]
}

type User {
  id: ID!
  email: String!
  userName: String!
  tweets: [Tweet]
}

type Tweet {
  content: String!
  date: String!
  likes: Int!
  user: User!
}